# MyRisk (금융거래 통합정보) - Docker Compose
# 사용: docker-compose up -d
# 중지: docker-compose down

services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8080
    restart: unless-stopped
