지금 기준으로 다음 해야 할 일 1순위는
analysis_basic 페이지들(은행/카드/금융정보)의 sticky 헤더를 split(헤더/본문 분리) 방식으로 통일하는 작업입니다.
문서상 이 구간만 “미적용(선택)”으로 남아 있고, 나머지 주요 테이블은 이미 해결 완료 상태라서 우선순위가 가장 높습니다.테이블_헤더_비침_해결_확인_전체.md:16-22 테이블_헤더_비침_해결_확인_전체.md:33-35 테이블_헤더_비침_해결_확인_전체.md:48-49 테이블_헤더_비침_해결_확인_전체.md:64-65

그 다음 순서는 이렇게 추천합니다.

_json_safe 경량화
리팩터링 검토 문서에서 우선 권장으로 남아 있고, 현재도 bank/card/cash 응답 경로에서 반복 호출되고 있어 체감 성능 개선 여지가 있습니다.로딩시간_리팩토링_검토.md:125-132 bank_app.py:656-664 card_app.py:692-700 cash_app.py:894-902

lib 공개 함수 타입 힌트·docstring 점진 적용
“미적용(선택)”으로 명시된 유지보수 과제입니다.코드_개선안_일괄_제안.md:57-64 코드_개선안_일괄_제안.md:102-103

참고로 성능 관련 큰 축(페이지네이션, gzip, orjson 경로)은 이미 코드/설정에 들어가 있습니다.

gzip after_request 적용.app.py:233-268
orjson provider 적용 시도 + fallback 처리, requirements에도 orjson 명시.app.py:88-101 requirements.txt:13-14
현재 브랜치는 깨끗하고, 열려 있는 이슈/PR도 없는 상태라(출력 공란) 위 문서 기반 우선순위로 진행하는 게 자연스럽습니다.terminal:1-8

원하시면 제가 바로 **1순위(analysis_basic 헤더 split 통일)**부터 실제 코드 수정까지 진행하겠습니다.


좋습니다. 바로 analysis_basic 3개 화면(MyBank/MyCard/MyCash)의 테이블 구조를 점검해서 sticky 방식인 구간을 split(헤더/본문 분리)로 바꾸고, 적용 후 수동 UI 테스트(영상 포함)까지 진행하겠습니다. 먼저 각 파일의 현재 테이블 마크업/CSS/JS 패턴을 읽겠습니다.

현재 구조를 보니 analysis_basic은 아직 sticky 헤더가 남아 있습니다. 동일 저장소에서 이미 해결된 split 패턴(index/category 페이지)을 가져오면 안전하게 적용할 수 있어서, 그 레퍼런스 마크업/CSS를 먼저 확인한 뒤 같은 방식으로 3개 파일에 반영하겠습니다.

이제 실제 수정 설계를 확정했습니다. analysis_basic에서 sticky 헤더/푸터가 있는 테이블을 split 구조(헤더 테이블 + 본문 스크롤 + 하단 합계 바)로 바꾸되, 기존 JS가 쓰는 ID(...-body, 합계 ID)는 그대로 유지해서 기능 회귀를 막겠습니다. 반영 전에 출력/스크립트가 tfoot에 의존하는지 마지막으로 확인하겠습니다.

이제 MyCash/templates/analysis_basic.html를 같은 방식으로 수정합니다. risk-classification/risk-detail 둘 다 split 구조로 바꿔 sticky 헤더/푸터 없이 동일한 UI를 유지하겠습니다.

구현 변경은 완료되어, 지시사항대로 테스트 전에 먼저 커밋/푸시를 진행하겠습니다. 그 다음 바로 서버 구동 후 UI 수동 테스트(영상 + 스크린샷)로 검증하겠습니다.

코드 반영은 끝났고, 이제 실제 동작 검증 단계로 들어갑니다. computerUse로 서버를 띄운 뒤 bank/card/cash 분석 화면에서 스크롤하면서 헤더/푸터가 정상 분리되는지 수동 테스트하고, 바로 데모 영상을 만들겠습니다.



