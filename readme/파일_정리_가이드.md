# MyRisk 파일 정리 가이드

유지보수·일회성 정리 방안과, 코드 실행에 직접 관여하지 않지만 참고용으로 두는 항목을 정리한 문서입니다.

---

## 1. 프로젝트 파일 구조 (유지보수용)

### 실행에 필수인 파일

| 경로 | 역할 |
|------|------|
| `app.py` | 통합 Flask 앱. MyBank·MyCard·MyCash 서브앱 등록. |
| `start_web.py` | Railway 등 배포 시 진입점. `Procfile`: `web: python start_web.py` |
| `MyBank/bank_app.py`, `MyBank/process_bank_data.py` | 은행 거래 통합·전처리·후처리·bank_after 생성 |
| `MyCard/card_app.py`, `MyCard/process_card_data.py` | 카드 거래 통합·전처리·후처리·card_after 생성 |
| `MyCash/cash_app.py`, `MyCash/process_cash_data.py` | 금융정보 종합·cash_after 생성 |
| `category_table_io.py` | category_table.json 읽기/쓰기·내보내기 |
| `category_table_defaults.py`, `category_constants.py` | 카테고리 기본값·상수 |
| `category_table_fallback.py` | category_table_io 로드 실패 시 폴백 |
| (업종분류·위험도) | lib/category_table_io로 통합 |
| `templates/` | 루트 공통 템플릿 (index, help 등) |

### 설정·배포 (실행에는 간접 관여)

| 경로 | 역할 |
|------|------|
| `requirements.txt`, `runtime.txt` | Python 의존성·버전 |
| `Procfile`, `Dockerfile`, `nixpacks.toml` | Railway 등 빌드·실행 방식 |
| `VERSION` | 버전 문자열 (app에서 읽음) |

### 실행에 불필요·유틸/일회성 (루트)

| 경로 | 역할 | 비고 |
|------|------|------|
| `run_full_flow.py` | 은행 ensure → 카드/캐시 로드 → 서버 기동 | 로컬 "전체 수행" 검증용. 삭제해도 앱 실행에는 영향 없음. |
| `start-server.ps1`, `start-server.bat` | 로컬 서버 기동 | `python app.py`와 동일. |

### readme/ 에 보관된 유틸 (참고/필요 시 실행)

| 경로 | 역할 | 실행 방법 |
|------|------|-----------|
| `data/category_table_j2x_backup.py` | category_table.json → xlsx 내보내기 (data 폴더 백업) | 프로젝트 루트에서 `python data/category_table_j2x_backup.py` |
| `add_virtual_asset_category.py` | 가상자산 회사 목록을 category_table에 추가 | 프로젝트 루트에서 `python "readme/add_virtual_asset_category.py"` |
| `setup-git-utf8.ps1` | Git 한글 커밋 인코딩 설정 | `.\readme\setup-git-utf8.ps1` |
| `create-workspace-junction.ps1` | C:\CursorWorkspace 정션 생성 (한글 경로 회피) | `.\readme\create-workspace-junction.ps1` |
| `start-cursor-no-hangul.ps1` | TEMP 한글 제거 후 Cursor 실행 | `.\readme\start-cursor-no-hangul.ps1` |

---

## 2. 일회성 코드 정리 방안

- **정리 시점**: 기능 추가/배포가 안정된 뒤 한 번만 수행하면 됨.
- **권장 순서**:
  1. **불필요 파일 제거**: 루트의 임시 메시지 파일(`msg*.txt`, `commit_msg*.txt`), 빈 폴더(`api/`, `image/` 등) 삭제. (본 가이드 반영 시 이미 제거됨.)
  2. **로그·임시 파일**: `run_full_flow.log` 등 `*.log`는 `.gitignore`에 포함되어 있으므로 커밋되지 않게 둠.
  3. **문서 정리**: 참고용 문서는 모두 `readme/`에만 두고, 루트에는 `README.md`만 유지.
  4. **유틸 스크립트**: category_table 백업은 `data/category_table_j2x_backup.py`, `.source/category_table_x2j_backup.py` 사용. `add_virtual_asset_category.py`는 `readme/`에 있음. 프로젝트 루트에서 실행. `run_full_flow.py`는 루트에 유지.
  5. **코드 내부**: 중복 함수·사용되지 않는 변수·과한 주석은 [코드_개선_조언.md](코드_개선_조언.md)에 따라 필요 시 단계적으로 정리.

---

## 3. 코드 실행에 직접 관여하지 않지만 필요한 것 (readme 보관)

- **문서**  
  - `readme/` 아래 모든 `.md`: Git·Railway·한글 경로·category_table 복구·코드 개선 조언·파일 정리 가이드 등.  
  - `금융_업종_API_참고.txt`: 업종코드·FIU·API·체크리스트 등 참고용. (구 `after_cash.xlsx를 생성하기전 업종코드가 데이터가 있.txt`를 이 이름으로 변경함.)
- **스크립트**  
  - `readme/` 안: `setup-git-utf8.ps1`, `create-workspace-junction.ps1`, `start-cursor-no-hangul.ps1`, `add_virtual_asset_category.py`. category_table 백업은 `data/category_table_j2x_backup.py`, `.source/category_table_x2j_backup.py` 사용.
- **배포 설정 설명**  
  - Git 호스팅·Railway 배포·Procfile·start_web.py 역할은 [Git_호스팅_가이드.md](Git_호스팅_가이드.md), [Railway_가입_배포_도메인.md](Railway_가입_배포_도메인.md)에 정리.

---

## 4. 삭제·이동한 파일 (정리 반영)

| 항목 | 처리 |
|------|------|
| `msg.txt` | 삭제 또는 무시. `.gitignore`에 포함. |
| `commit_msg_utf8.txt` | `commit_msg_*.txt`로 무시. |
| `run_full_flow.log` | `*.log`로 무시·삭제 가능. |
| `api/`, `image/` (빈 폴더) | 미사용. |
| `__pycache__/` (루트, MyBank, MyCard, MyCash, Scripts) | 불필요 시 삭제 가능. (검사 시 삭제함. 재실행 시 자동 재생성.) |
| `setup-git-utf8.ps1`, `create-workspace-junction.ps1`, `start-cursor-no-hangul.ps1` | **readme/** 로 이동. |
| `add_virtual_asset_category.py` | **readme/** 에 있음. (실행 시 프로젝트 루트에서 실행.) category_table 백업은 data/, .source/ 스크립트 사용. |

readme 폴더 내 문서는 **삭제하지 않음** 원칙에 따라 제거 대상에서 제외합니다.

---

## 5. 통합 정리

- **Python (category_*)**: `category_constants.py`, `category_table_io.py`, `category_table_defaults.py`, `category_table_fallback.py`는 역할별로 분리 유지. 무리한 통합은 하지 않음. (자세한 통합 가능성은 [category_통합_가이드.md](category_통합_가이드.md) 참고.)
- **HTML (category.html 3개)**: Bank/Card/Cash 각각 별도 템플릿 유지. 공통 base로 통합하려면 category_통합_가이드의 "방식 B" 참고.
